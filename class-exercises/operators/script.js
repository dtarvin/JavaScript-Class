var total = 0;
total = total + 2;
total += 3;
total++;
console.log("Total is " + total);

// division
console.log(8 / 5);

// modulus
console.log(8 % 5);
console.log(4 % 2);
console.log(3 % 2);

function divisionAndRemainder(num1, num1) {
  console.log(mum1 + " divided by " + num2 + " is " + num1 / num2 + " with " + (num1 % num2) + " left over.");
}
// 3 x 3 = 9, so if we divide 10 by 3, 3 goes into 10 3 times 1 left over
divisionAndRemainder(10, 3);
//4 x 3 = 12, 10 divide 14 by 4, we get 3 with 2 left over
divisionAndRemainder(14, 4);
//5 x 6 = 30, if we divide 30 by 5, we get 6 with 0 left over
divisionAndRemainder(30, 6);

